import { makeStyles } from '@material-ui/core/styles';

const stylesContadorChecksDiarios = makeStyles((theme) => ({
    depositContext: {
        flex: 1,
    },
    button: {
        margin: theme.spacing(1),
        padding: theme.spacing(0.1),
    },
}));

export default stylesContadorChecksDiarios;
